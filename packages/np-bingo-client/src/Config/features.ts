const config = {
  'single-player': true,
  'share-room': false, // unfinished
  'public-rooms': false, // unfinished
  'game-modes': false, // no game logic built for alternate modes
  'special-rules': false, // no win conditions built for special rules
  'streamer-mode': false, // not implemented
  'max-room-size': 30,
  'max-active-games': 5,
};

export default config;
