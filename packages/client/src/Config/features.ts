const config = {
  'private-only': false, // Only allow private games
  'single-player': true,
  'share-room': true,
  'public-rooms': false, // Only mock-up
  'game-modes': false, // no game logic built for alternate modes
  'special-rules': false, // no win conditions built for special rules
  'streamer-mode': false, // not implemented
  'max-room-size': 30,
  'max-active-games': 5,
  'ball-delay': 3500,
};

export default config;
